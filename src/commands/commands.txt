Here is a template for commands:

module.exports = {
  category: 'Messaging',
  description: 'Command',
  ownerOnly: false,
  slash: 'both',
  options: [

  ],
  callback: async ({ guild, client, message, args, interaction }) => {
    if (message) {
      
    }

    if (interaction) {
      
    }
}
}